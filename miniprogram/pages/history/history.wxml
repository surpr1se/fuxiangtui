<view class="page"><view class="section-title">我的计算记录</view><view class="empty" wx:if="{{!list.length}}">暂无测算记录</view><view class="card history" wx:for="{{list}}" wx:key="id" bindtap="detail" data-i="{{index}}"><view><view class="h-title">{{item.title}}</view><view class="muted">{{item.time}}</view></view><view class="money">{{item.money}}</view></view></view>
